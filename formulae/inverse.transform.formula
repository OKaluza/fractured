//Invert the complex plane (works as pre-transform only)
//Center
@:center = complex(0,0);
//Radius
@:radius = real(1);

//Init, called before setting initial conditions
init:
  point = @:center + div(@:radius, (point - @:center));

