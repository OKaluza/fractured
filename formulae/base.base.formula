//Base parameter definitions
@iterations    = int(100); //Iterations
@outrepeat     = real(1);  //Repeat (Outside)
@inrepeat      = real(1);  //Repeat (Inside)
@antialias     = int(1);   //Anti-alias
@vary          = real(0);  //Vary Iterations
